# ElevenLabs connection status

This workspace preserves the provider-specific `ai-voiceover` craft guidance, but no ElevenLabs client, credential, billing route or paid plan is installed here. The zero-budget local route is narration import or the separately guarded Gemini adapter in `VoiceOver/gemini_tts.py`.

Do not add a provider call without verifying current pricing, consent, commercial rights and authorization. A tool key is not proof of a free route. Treat this file as an explicit uninstalled integration boundary, not as an API implementation.

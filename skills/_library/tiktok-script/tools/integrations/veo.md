# Veo connection status

The TikTok script skill can describe a Veo prompt pack, but this workspace does not install a Veo client, model, credential or cloud-rendering path. The local operator uses editable SVG frames and FFmpeg for the setup proof. Treat any generated-video route as a separate, explicitly authorized integration; never upload a confidential story or activate paid usage by inference.
